/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppHome from './FrontEnd/SocialMediaApps';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppHome);
